# react_tailwind-project
A modern React + Tailwind CSS project built by cloning the Motonic website layout. This project converts the original Motonic HTML template into reusable React components using Vite for fast development. Includes responsive UI, clean structure, and well-organized assets.
